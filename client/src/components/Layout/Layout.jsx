import React from 'react';
import {Container} from "@mui/material";

export default function ({children}) {
    return (
        <Container>
            {children}
        </Container>
    );
};

